# WES OS — Company Engine frontend environment
# Copy to `.env`. Base URL of the backend API.
# Leave empty to use the Vite dev proxy (/api -> http://localhost:8000).
VITE_API_BASE_URL=

# Backend origin the Vite dev server proxies /api to (development only).
VITE_API_PROXY=http://localhost:8000
